import 'package:flutter/material.dart';

class FavoriteFragementScreen extends StatelessWidget {
  const FavoriteFragementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}